package jorge.iphone;

public class BasicCamera extends AbstractCamera {

    public BasicCamera(String model, int noCamera, String resolution, double price, String version) {
        super(model, noCamera, resolution, price, version);
    }
}
